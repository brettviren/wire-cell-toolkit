# TestClusterShadow

Diagnostic component that computes blob and cluster shadow graphs from the input cluster, logs statistics to a file ("TestClusterShadow.log"), and passes the cluster through unchanged.

## Node Properties

| Property | Value |
| --- | --- |
| Factory name | `TestClusterShadow` |
| Concrete class | `WireCell::Img::TestClusterShadow` |
| Node category | function |
| Primary interface | `IClusterFilter` |
| Input type(s) | `ICluster` |
| Output type(s) | `ICluster` |
| Configurable | yes |

## Configuration Parameters

This component is configurable but has no documented parameters.

# SlicesSink

A no-op sink that accepts an ISliceFrame and discards it; intended as a placeholder or debugging endpoint for slice data.

## Node Properties

| Property | Value |
| --- | --- |
| Factory name | `SlicesSink` |
| Concrete class | `WireCell::Img::SlicesSink` |
| Node category | sink |
| Primary interface | `ISliceFrameSink` |
| Input type(s) | `(ISlice, IFrame)` |
| Output type(s) | `(none)` |
| Configurable | yes |

## Configuration Parameters

This component is configurable but has no documented parameters.

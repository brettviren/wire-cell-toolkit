// SteinerFunctions.cxx — free functions for Steiner-graph manipulation.
//
// improve_grapher / improve_grapher_1 / improve_grapher_2 are stubs reserved
// for a future port of the analogous prototype path
// (WCPPID::ImprovePR3DCluster.cxx, single-cluster overload).
// That prototype path is itself unused in the NeutrinoID pipeline, so these
// functions have no active call sites in the toolkit either.
// Do NOT call them; they exist only to document the intended port structure.

#include "SteinerFunctions.h"

// (intentionally no implementations — these are reserved stubs)

